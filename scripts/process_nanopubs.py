#!/usr/bin/env python3
"""
Science Live Orchestrator
This script uses the nanopub-content-generator as an installed Python package to generate outputs from one or more nanopublications.
It handles async/await for the nanopub-content-generator package.
"""

import os
import json
import time
import asyncio
from datetime import datetime
from pathlib import Path

def setup_directories():
    """Create necessary directories for output"""
    directories = ["results", "logs", "config", "results/content"]
    for directory in directories:
        Path(directory).mkdir(exist_ok=True)
    print("✅ Output directories created")

async def run_content_generation_async(nanopub_urls, content_types, ai_model, user_instructions, batch_id, batch_description):
    """Run content generation using the installed package - PROPERLY ASYNC"""
    try:
        from nanopub_content_generator import NanopubContentGenerator
        print("✅ Successfully imported nanopub_content_generator package")
    except ImportError as e:
        print(f"❌ Failed to import nanopub_content_generator package: {e}")
        print("Make sure it's installed: pip install git+https://github.com/ScienceLiveHub/nanopub-content-generator.git")
        return []
    
    # Initialize the content generator
    generator = NanopubContentGenerator()
    generated_files = []
    
    # Process each content type
    for i, content_type in enumerate(content_types, 1):
        print(f"\n🎯 Processing {i}/{len(content_types)}: {content_type}")
        
        try:
            # FIXED: Properly await the async method
            result = await generator.run_pipeline(
                nanopub_uris=nanopub_urls,
                template_name=content_type,
                ollama_model=ai_model,
                user_instructions=user_instructions or f"Create engaging {content_type.replace('_', ' ')} content that maintains scientific accuracy while being accessible to the target audience.",
                description=f"{batch_description or 'Science Live content generation'} - {content_type}"
            )
            
            if "error" in result:
                print(f"❌ Content generation failed for {content_type}: {result['error']}")
                error_file = create_error_file(content_type, batch_id, result['error'])
                if error_file:
                    generated_files.append(error_file)
            else:
                print(f"✅ Content generation successful for {content_type}")
                
                # Save the generated content
                content_file = save_generated_content(
                    content=result['generated_content'],
                    content_type=content_type,
                    batch_id=batch_id,
                    citations=result.get('source_citations', ''),
                    metadata=result.get('metadata', {})
                )
                
                if content_file:
                    generated_files.append(content_file)
                    print(f"📄 Saved: {content_file}")
        
        except Exception as e:
            print(f"❌ Error processing {content_type}: {e}")
            import traceback
            traceback.print_exc()
            error_file = create_error_file(content_type, batch_id, str(e))
            if error_file:
                generated_files.append(error_file)
    
    return generated_files

def save_generated_content(content, content_type, batch_id, citations="", metadata=None):
    """Save generated content to a file"""
    filename = f"results/content/{content_type}_{batch_id}.txt"
    
    try:
        os.makedirs(os.path.dirname(filename), exist_ok=True)
        
        with open(filename, 'w', encoding='utf-8') as f:
            # Write header
            f.write(f"# {content_type.upper().replace('_', ' ')} CONTENT\n")
            f.write(f"Generated by Science Live Content Generator\n")
            f.write(f"Batch ID: {batch_id}\n")
            f.write(f"Content Type: {content_type}\n")
            f.write(f"Generated: {datetime.now().isoformat()}\n\n")
            f.write("=" * 60 + "\n\n")
            
            # Write main content
            f.write(content)
            
            # Write citations if available
            if citations:
                f.write(f"\n\n" + "=" * 60)
                f.write(f"\nSOURCE CITATIONS\n")
                f.write("=" * 60 + "\n\n")
                f.write(citations)
            
            # Write metadata if available
            if metadata:
                f.write(f"\n\n" + "=" * 60)
                f.write(f"\nMETADATA\n")
                f.write("=" * 60 + "\n\n")
                for key, value in metadata.items():
                    f.write(f"{key}: {value}\n")
            
            f.write(f"\n\n" + "=" * 60)
            f.write(f"\nGenerated: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
        
        return filename
        
    except Exception as e:
        print(f"❌ Error saving content for {content_type}: {e}")
        return None

def create_error_file(content_type, batch_id, error_message):
    """Create an error file when content generation fails"""
    error_file = f"results/content/{content_type}_{batch_id}_ERROR.txt"
    
    try:
        os.makedirs(os.path.dirname(error_file), exist_ok=True)
        
        with open(error_file, 'w', encoding='utf-8') as f:
            f.write(f"# ERROR: {content_type.upper().replace('_', ' ')}\n\n")
            f.write(f"Content generation failed for {content_type}\n\n")
            f.write(f"Error Details:\n")
            f.write(f"{error_message}\n\n")
            f.write(f"Batch ID: {batch_id}\n")
            f.write(f"Content Type: {content_type}\n")
            f.write(f"Generated: {datetime.now().isoformat()}\n\n")
            f.write("TROUBLESHOOTING:\n")
            f.write("- Check that nanopub URLs are accessible\n")
            f.write("- Verify AI model is available in Ollama\n")
            f.write("- Check network connectivity\n")
            f.write("- Review template configuration\n")
        
        return error_file
        
    except Exception as e:
        print(f"❌ Could not create error file: {e}")
        return None

async def async_main():
    """Async main function - FIXED"""
    print("=== SCIENCE LIVE SIMPLE FIXED ORCHESTRATOR ===")
    print("🎯 Using nanopub-content-generator package with proper async handling")
    print("🔧 FIXED: Async/await issues resolved")
    start_time = time.time()
    
    # Get environment variables
    nanopub_urls_str = os.getenv('NANOPUB_URLS', '')
    batch_id = os.getenv('BATCH_ID', f'batch_{int(time.time())}')
    content_types_str = os.getenv('CONTENT_TYPES', 'linkedin_post')
    ai_model = os.getenv('AI_MODEL', 'llama3:8b')
    user_instructions = os.getenv('USER_INSTRUCTIONS', '')
    batch_description = os.getenv('BATCH_DESCRIPTION', '')
    enable_content_generation = os.getenv('ENABLE_CONTENT_GENERATION', 'true').lower() == 'true'
    
    if not nanopub_urls_str:
        print("❌ ERROR: No nanopublication URLs provided")
        print("Usage: NANOPUB_URLS='url1,url2' python scripts/process_nanopubs.py")
        return
    
    nanopub_urls = [url.strip() for url in nanopub_urls_str.split(',') if url.strip()]
    content_types = [ct.strip() for ct in content_types_str.split(',') if ct.strip()]
    
    print(f"📊 Processing {len(nanopub_urls)} nanopublications")
    print(f"📝 Selected nanopublications:")
    for i, url in enumerate(nanopub_urls, 1):
        print(f"   {i}. {url}")
    print(f"🎯 Content types: {', '.join(content_types)}")
    print(f"🤖 AI Model: {ai_model}")
    print(f"🆔 Batch ID: {batch_id}")
    print(f"🎨 Content generation: {'ENABLED' if enable_content_generation else 'DISABLED'}")
    print(f"📦 Method: Package import with proper async handling")
    
    setup_directories()
    
    generated_files = []
    
    if enable_content_generation:
        print("\n🚀 Starting content generation with proper async handling...")
        
        # Run content generation using the package (PROPERLY ASYNC)
        generated_files = await run_content_generation_async(
            nanopub_urls, content_types, ai_model, user_instructions, batch_id, batch_description
        )
    else:
        print("ℹ️  Content generation disabled")
    
    # Create results summary
    total_time = time.time() - start_time
    
    summary = {
        'batch_id': batch_id,
        'processing_method': 'python_package_async_fixed',
        'package_name': 'nanopub-content-generator',
        'total_nanopubs': len(nanopub_urls),
        'selected_nanopub_urls': nanopub_urls,
        'content_types_requested': content_types,
        'content_generated': len(generated_files),
        'processing_time': total_time,
        'ai_model': ai_model,
        'user_instructions': user_instructions,
        'batch_description': batch_description,
        'generated_files': generated_files,
        'content_generation_enabled': enable_content_generation,
        'successful_templates': len([f for f in generated_files if 'ERROR' not in f]),
        'failed_templates': len([f for f in generated_files if 'ERROR' in f]),
        'timestamp': datetime.now().isoformat(),
        'architecture': 'simple_package_based_async_fixed'
    }
    
    # Save results
    with open('results/batch_results.json', 'w', encoding='utf-8') as f:
        json.dump(summary, f, indent=2, ensure_ascii=False)
    
    # Create processing summary
    success_count = len([f for f in generated_files if 'ERROR' not in f])
    error_count = len([f for f in generated_files if 'ERROR' in f])
    
    report = f"""=== SCIENCE LIVE SIMPLE FIXED ORCHESTRATION RESULTS ===
Generated: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}
Batch ID: {batch_id}

=== SIMPLE ARCHITECTURE SUMMARY (FIXED) ===
📦 Package: nanopub-content-generator (installed Python package)
🔧 Fix Applied: Proper async/await handling for run_pipeline()
🏗️  Architecture: Direct package import with async orchestration
📊 Nanopublications: {len(nanopub_urls)}
🎯 Content Types: {', '.join(content_types)}
🤖 AI Model: {ai_model}
⏱️  Processing Time: {total_time:.2f} seconds
✅ Success Rate: {success_count}/{len(content_types)} content types
❌ Failed: {error_count}/{len(content_types)} content types

=== GENERATED CONTENT ===
{chr(10).join(f"📄 {os.path.basename(f)}" for f in generated_files) if generated_files else "No content files generated"}

=== CONFIGURATION ===
📝 User Instructions: {user_instructions or 'Default high-quality standards'}
📋 Batch Description: {batch_description or 'Science Live package-based generation'}

=== SIMPLE ARCHITECTURE BENEFITS (FIXED) ===
✅ Direct Package Import: Clean import and async function calls
✅ Proper Async Handling: Uses asyncio.run() for event loop management
✅ No CLI Complexity: Direct Python package usage
✅ Better Error Handling: Async exceptions properly caught
✅ Same as Local: Identical behavior to your working local setup
✅ Environment Variables: Uses same env vars as your local setup

=== PACKAGE DETAILS ===
Installation: pip install git+https://github.com/ScienceLiveHub/nanopub-content-generator.git
Import: from nanopub_content_generator import NanopubContentGenerator
Usage: await generator.run_pipeline(...)  # PROPERLY AWAITED NOW
Event Loop: asyncio.run(async_main())  # PROPER ASYNC HANDLING

Generated files: {len(generated_files)}
Architecture status: {'SUCCESS' if success_count > 0 else 'NEEDS_ATTENTION'}
Package integration: {'WORKING' if success_count > 0 else 'CHECK_INSTALLATION'}
Async handling: FIXED
"""
    
    with open('logs/processing_summary.txt', 'w', encoding='utf-8') as f:
        f.write(report)
    
    print(report)
    
    if success_count > 0:
        print("=== ✅ SIMPLE FIXED ORCHESTRATION SUCCESSFUL ===")
        print(f"Successfully generated {success_count} content types using direct package import")
        print("🔧 Async issue fixed: Proper await handling implemented")
        print("🏗️  Simple architecture: Same as your working local setup")
        
        # Show content previews
        for file_path in [f for f in generated_files if 'ERROR' not in f][:2]:
            try:
                with open(file_path, 'r', encoding='utf-8') as f:
                    content = f.read()
                    print(f"\n📄 {os.path.basename(file_path)} ({len(content)} chars)")
                    print("=" * 50)
                    # Show meaningful preview (skip headers)
                    lines = content.split('\n')
                    content_lines = []
                    skip_header = True
                    for line in lines:
                        if skip_header and line.strip() and not line.startswith('=') and not line.startswith('#'):
                            skip_header = False
                        if not skip_header and line.strip():
                            content_lines.append(line)
                            if len(content_lines) >= 3:
                                break
                    preview = '\n'.join(content_lines)
                    print(preview[:200] + "..." if len(preview) > 200 else preview)
            except Exception as e:
                print(f"⚠️  Could not preview {file_path}: {e}")
    else:
        print("=== ❌ SIMPLE FIXED ORCHESTRATION FAILED ===")
        print("No content was successfully generated")
        print("Check that:")
        print("- nanopub-content-generator package is installed")
        print("- Ollama is running with the required AI model")
        print("- Nanopublication URLs are valid and accessible")
        print("- Package dependencies are satisfied")
        print("\nInstallation command:")
        print("pip install git+https://github.com/ScienceLiveHub/nanopub-content-generator.git")

def main():
    """Main synchronous entry point - FIXED ASYNC HANDLING"""
    try:
        # CRITICAL FIX: Use asyncio.run() to properly handle the async main function
        asyncio.run(async_main())
    except KeyboardInterrupt:
        print("\n🛑 Process interrupted by user")
    except Exception as e:
        print(f"\n❌ Error: {e}")
        import traceback
        traceback.print_exc()

if __name__ == "__main__":
    main()
